diff --git a/node_modules/@svar-ui/lib-react/dist/index.cjs b/node_modules/@svar-ui/lib-react/dist/index.cjs
index 724bb4c..73e0518 100644
--- a/node_modules/@svar-ui/lib-react/dist/index.cjs
+++ b/node_modules/@svar-ui/lib-react/dist/index.cjs
@@ -19,15 +19,30 @@ function useWritableProp(propValue) {
 }
 function useWritable(writable2, initialValue, name) {
   const [state, setState] = react.useState(() => initialValue);
+  const latestRef = react.useRef(initialValue);
+  const rafRef = react.useRef(null);
   if (!writable2) console.warn(`Writable ${name} is not defined`);
   react.useEffect(() => {
     if (!writable2) return;
-    const unsubscribe = writable2.subscribe((newValue) => {
-      setState(() => newValue);
+    let ready = false;
+    writable2.subscribe((newValue) => {
+      latestRef.current = newValue;
+      if (ready && rafRef.current === null) {
+        rafRef.current = requestAnimationFrame(() => {
+          rafRef.current = null;
+          setState(() => latestRef.current);
+        });
+      }
     });
-    return unsubscribe;
+    Promise.resolve().then(() => { ready = true; });
+    return () => {
+      if (rafRef.current !== null) {
+        cancelAnimationFrame(rafRef.current);
+        rafRef.current = null;
+      }
+    };
   }, [writable2]);
-  return state;
+  return latestRef.current;
 }
 function useStore(store, name) {
   const s = store.getState();
@@ -50,12 +65,25 @@ function useWritableWithCounter(writable2, initialValue) {
   const state = react.useRef(initialValue);
   state.current = initialValue;
   const [counter, setCounter] = react.useState(1);
+  const rafRef = react.useRef(null);
   react.useEffect(() => {
-    const unsubscribe = writable2.subscribe((newValue) => {
+    let ready = false;
+    writable2.subscribe((newValue) => {
       state.current = newValue;
-      setCounter((v) => v + 1);
+      if (ready && rafRef.current === null) {
+        rafRef.current = requestAnimationFrame(() => {
+          rafRef.current = null;
+          setCounter((v) => v + 1);
+        });
+      }
     });
-    return unsubscribe;
+    Promise.resolve().then(() => { ready = true; });
+    return () => {
+      if (rafRef.current !== null) {
+        cancelAnimationFrame(rafRef.current);
+        rafRef.current = null;
+      }
+    };
   }, [writable2]);
   return [state.current, counter];
 }
diff --git a/node_modules/@svar-ui/lib-react/dist/index.js b/node_modules/@svar-ui/lib-react/dist/index.js
index 9b665b5..0fd72f2 100644
--- a/node_modules/@svar-ui/lib-react/dist/index.js
+++ b/node_modules/@svar-ui/lib-react/dist/index.js
@@ -17,15 +17,30 @@ function useWritableProp(propValue) {
 }
 function useWritable(writable2, initialValue, name) {
   const [state, setState] = useState(() => initialValue);
+  const latestRef = useRef(initialValue);
+  const rafRef = useRef(null);
   if (!writable2) console.warn(`Writable ${name} is not defined`);
   useEffect(() => {
     if (!writable2) return;
-    const unsubscribe = writable2.subscribe((newValue) => {
-      setState(() => newValue);
+    let ready = false;
+    writable2.subscribe((newValue) => {
+      latestRef.current = newValue;
+      if (ready && rafRef.current === null) {
+        rafRef.current = requestAnimationFrame(() => {
+          rafRef.current = null;
+          setState(() => latestRef.current);
+        });
+      }
     });
-    return unsubscribe;
+    Promise.resolve().then(() => { ready = true; });
+    return () => {
+      if (rafRef.current !== null) {
+        cancelAnimationFrame(rafRef.current);
+        rafRef.current = null;
+      }
+    };
   }, [writable2]);
-  return state;
+  return latestRef.current;
 }
 function useStore(store, name) {
   const s = store.getState();
@@ -48,12 +63,25 @@ function useWritableWithCounter(writable2, initialValue) {
   const state = useRef(initialValue);
   state.current = initialValue;
   const [counter, setCounter] = useState(1);
+  const rafRef = useRef(null);
   useEffect(() => {
-    const unsubscribe = writable2.subscribe((newValue) => {
+    let ready = false;
+    writable2.subscribe((newValue) => {
       state.current = newValue;
-      setCounter((v) => v + 1);
+      if (ready && rafRef.current === null) {
+        rafRef.current = requestAnimationFrame(() => {
+          rafRef.current = null;
+          setCounter((v) => v + 1);
+        });
+      }
     });
-    return unsubscribe;
+    Promise.resolve().then(() => { ready = true; });
+    return () => {
+      if (rafRef.current !== null) {
+        cancelAnimationFrame(rafRef.current);
+        rafRef.current = null;
+      }
+    };
   }, [writable2]);
   return [state.current, counter];
 }
